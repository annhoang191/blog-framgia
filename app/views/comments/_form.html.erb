<%= form_for [@post, @post.comments.build], remote: true do |f| %>
  <div class="field form-group">
    <%= f.text_area :content, rows: "5" %>
  </div>
  <div class="actions">
    <%= f.submit t(".comment"), class:"W2Gbutton" %>
  </div>
<% end %>
